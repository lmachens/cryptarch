export const printWelcomeMessage = (): void => {
  console.log('Welcome to cryptarch (⌐■_■)');
};

export const printInvalidMainPassword = (): void => {
  console.error('The main password is invalid ￣へ￣');
};

export const printCorrectMainPassword = (): void => {
  console.error('The main password is correct (❁´◡`❁)');
};
